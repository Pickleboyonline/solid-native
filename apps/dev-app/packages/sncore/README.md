Android lives in android-sncore

iOS lives in build/swift/SNCore

Use the just build commands to build them.

The rational for project structure is because iOS is completely auto-gen whereas Android needs some of my config.