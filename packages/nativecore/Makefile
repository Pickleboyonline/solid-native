build-ios:
	@# TODO: make build iOS script and ensure to go get correct binaries for go mobile
	@echo hello world!

clean-build:
	rm -rf ./build

build: clean-build
	go build -o build/bin/main main.go