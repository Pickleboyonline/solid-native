/**
 * @file Meant to be for stubs. Want similar API to React Navigation.
 */

export function createStackNavigator(config: unknown) {
  return () => "Hello World!";
}

export function createNavigation(input: unknown) {
  return input;
}
