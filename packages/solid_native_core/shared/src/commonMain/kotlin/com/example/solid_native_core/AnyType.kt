package com.example.solid_native_core

/**
 * Acts as a wrapper for any type. This is to ensure Kotlin signatures are the same
 * for Swift to reduce confusion.
 * Type checking happens on the target platform, where
 */
class AnyType {
}